# 2026-02-21 — Action Handoff (v4)

## Objective

Execute the approved v4 plan from a frozen evidence base, then enter paper drafting
with claims locked to available evidence strength.

## Phase Order (Do Not Reorder)

1. Phase 1 — pipeline fix (TDD)
2. Phase 2 — data completion (`none`)
3. Phase 3 — analysis gate + rerun decision
4. Phase 4 — claims lock enforcement in writing
5. Phase 5 — final handoff pack

## Phase 1 Checklist (TDD Required)

- [ ] Add failing tests for method-scoped index build behavior:
  - GM-family builds graph only.
  - RAG-family builds rag only.
  - none/oracle build neither.
- [ ] Add failing tests for new summary fields:
  - `setup_tokens_graph_built`
  - `setup_tokens_rag_built`
  - `setup_tokens_method_accounted`
- [ ] Implement split build logic in `run_patch.py`.
- [ ] Keep existing fields for backward compatibility:
  - `retrieval_setup_tokens`, `total_cost_tokens` unchanged.
- [ ] Run full suite: `./.venv/bin/python -m unittest discover -s tests -v`.

## Phase 2 Checklist (Completion Gate)

A repo/method cell is complete when `patch_summary.json` has either:

- parsed `harness_results` with `n_resolved`, or
- explicit `harness_error`, or
- explicit `harness_skipped_reason`.

- [ ] Confirm all 9 `none` repo runs meet completion criteria.

## Phase 3 Required Outputs (Single Script)

Write outputs under: `results/analysis_v4_handoff/`

- [ ] `resolved_rate_4method_table.csv`
- [ ] `pooled_cpr_table.csv`
- [ ] `dual_accounting_table.csv`
- [ ] `timeout_sensitivity_table.csv`
- [ ] `mcnemar_gm_vs_rag.json`
- [ ] `discordant_instances.csv`
- [ ] `rerun_gate_decision.md`

## Rerun Gate (Binary)

Trigger targeted rerun (sympy/sphinx/matplotlib for GM+RAG) only if any hold:

1. CPR ranking reverses between method-accounted and as-run-consumed rows.
2. Timeout sensitivity shifts GM-RAG resolved gap by more than 5 percentage points.
3. Timeout-confounded majority among discordant instances:
   winner resolved, loser did not, and loser `patch_status == timeout_budget_exceeded`.

## Phase 4 Writing Constraints

- [ ] Enforce `CLAIMS_LOCK.md` in all report edits.
- [ ] Keep cost-efficiency as thesis; patching superiority language remains exploratory.

## Phase 5 Final Pack

- [ ] Update `CURRENT_STATE.md` with phase completion + artifact paths + rerun gate outcome.
- [ ] Add handoff dev log summarizing final metrics and claim-safe narrative.
- [ ] Ensure `RESEARCH_INTENT.md` unchanged unless researcher explicitly approves edits.

## Key References

- `CURRENT_STATE.md`
- `CLAIMS_LOCK.md`
- `RESEARCH_INTENT.md`
- `run_patch.py`
- `tests/test_patch_runner.py`
